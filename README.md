# FIM-Application

## Overview

I created an FIM app thats primary goal is to detect changes in specified folders(added, deleted, Modified). 
The first version will not use CLI commands for test purposes but i will integrste the script from the CLI 


Goals:

Create an FIM application that works on Linux and Windows that detects, Logs and Notifies. Given a folder on a laptop it detects the changes made in that follder with information which file and what time. This will be done using tools as SHA256, 





Working Steps:
For this project i worked in small steps building on each toher to create the finished end result to ensure small errors along the way do not end up taking much troubleshooting later one so testing each independed step is crutual
Create SHA256 Hasher